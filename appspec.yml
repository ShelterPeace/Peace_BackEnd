version: 0.0
os: linux

files:
  - source: /
    destination: /home/ec2-user/xmpi_deploy
    overwrite: yes

permissions:
  - object: /
    pattern: "**"
    owner: ec2-user
    group: ec2-user

hooks:
  ApplicationStart:
    - location: scripts/deploy.sh
      timeout: 30
      runas: ec2-user